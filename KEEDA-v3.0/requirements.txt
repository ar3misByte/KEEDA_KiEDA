# KiCad Live - Python dependencies
# Tested on Python 3.14.7 (Windows 11) against KiCad 10.0.6.

# --- Server (Computer 1) ---
fastapi==0.121.2
uvicorn==0.41.0
websockets==15.0.1

# --- Agent (each designer's computer) ---
# Talks to a running KiCad over its IPC API. Requires KiCad 9 or newer.
kicad-python==0.8.0

# --- Tests ---
pytest==8.4.2
pytest-asyncio==1.3.0
httpx==0.28.1
